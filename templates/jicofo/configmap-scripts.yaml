{{- if .Values.jicofo.custom.scripts._config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "jitsi-meet.jicofo.fullname" . }}-scripts
  labels:
    {{- include "jitsi-meet.jicofo.labels" . | nindent 4 }}
data:
  config: |
    {{- .Values.jicofo.custom.scripts._config | nindent 4 }}
{{- end }}
